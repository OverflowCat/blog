---
export type Props = {
  tag: string;
  text: string;
};

import { crc32base } from "@/scripts/serendipity";
const Element = Astro.props.tag;
const ident = crc32base(Astro.props.text);
---

<Element set:text={Astro.props.text} transition:name={ident} />
